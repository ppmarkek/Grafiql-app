import { Grid } from '@mui/material';
import { Wrapper } from './style';

const Header = () => {
  return (
    <Wrapper container>
      <Grid>da</Grid>
    </Wrapper>
  );
};

export default Header;
